# ==========================================
# PhotoArchiver - Runtime Dependencies
# ==========================================

# ---------- GUI ----------
PySide6==6.11.1

# ---------- Data ----------
# pandas: approved but unused — currently zero import (openpyxl used instead).
# Step 14 Export uses openpyxl directly. pandas reserved for future stats/reporting.
pandas==2.3.1
openpyxl==3.1.5

# ---------- Image ----------
# numpy: direct runtime dependency — hard requirement of opencv-python and
# insightface at import time (P2-009). Pinned explicitly instead of relying
# on transitive resolution so upgrades cannot silently break the AI stack.
numpy==2.2.6
opencv-python==4.12.0.88
Pillow==11.3.0

# ---------- AI ----------
insightface==1.0.1
onnxruntime==1.27.0

# ---------- Database ----------
# SQLAlchemy + Alembic are active via alembic_runner.py (ADR-024).
# SQLAlchemy Engine is used by Alembic to connect to the database.
# The ORM is NOT used — repositories still use raw sqlite3. Do not remove.
SQLAlchemy==2.0.43
alembic==1.16.4

# ---------- Configuration ----------
pydantic==2.11.7
pydantic-settings==2.10.1

# ---------- Networking ----------
# certifi: CA bundle for the model downloader's TLS (P0-8 release blocker).
# CPython's default CA loading failed on a clean Windows VM (no issuer for
# the github.com chain); certifi's bundle verified. Already present
# transitively — promoted to a declared runtime dependency so a clean
# machine always has the CA bundle.
certifi==2026.6.17

# ---------- Logging ----------
loguru==0.7.3

# ---------- File Watching ----------
# watchdog: approved but unused — filesystem watcher用途预留,currently zero import.
# Do not remove without confirming the approval is superseded.
watchdog==6.0.0
